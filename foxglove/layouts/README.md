# Foxglove layouts

Export shared Foxglove layouts into this directory when the project starts
maintaining standard engineering views. Layouts should refer to stable topic
names and should be documented in `../README.md`.

The temperature-route demo currently uses a user script rather than a committed
layout because Foxglove user scripts are stored inside a layout and are most
easily created from the Foxglove interface.
